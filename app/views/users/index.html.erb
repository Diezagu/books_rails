<h1>Authors</h1>
<% @users.each do |user| %>
  <p> <%= user.name %> </p>
  <p> <%= user.age %> </p>
  <%= link_to 'Show', user_path(user) %>
<% end %>
<div>
  <%= link_to 'Download Report', users_path(format: :csv) %>
</div>

